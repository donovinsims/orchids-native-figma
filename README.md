
  # native

  This is a code bundle for native. The original project is available at https://www.figma.com/design/tKfoFlBjeYrT6WAcwIaFFA/native.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  